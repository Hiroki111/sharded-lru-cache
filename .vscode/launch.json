{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Cache Server",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/cache-server/main.go"
        }
    ]
}